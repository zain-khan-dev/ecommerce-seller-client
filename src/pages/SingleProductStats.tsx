import { useAuthenticator } from "../utils/utilityFunc"

const SingleProductStats = () => {

    const isLogged = useAuthenticator()

    return(
        <div>Single product stats will be shown here</div>
    )
}

export default SingleProductStats